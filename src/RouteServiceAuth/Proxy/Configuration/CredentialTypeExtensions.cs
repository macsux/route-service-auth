using System;
using Microsoft.AspNetCore.Authentication.OAuth;

namespace RouteServiceAuth.Proxy.Configuration
{
    public static class CredentialTypeExtensions
    {
        // public static Type? GetCredentialClassType(this Credential.CredentialType type) => type switch
        // {
        //     Credential.CredentialType.OAuth => typeof(OAuthCredential),
        //     Credential.CredentialType.Windows => typeof(WindowsCredential),
        //     _ => null
        // };
        
        
    }

}